name: backend_dart
description: CrisisSync Dart Backend — shelf + Firebase REST + Gemini
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.0
  shelf_cors_headers: ^0.1.5
  http: ^1.2.1
  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.9.0
  bcrypt: ^1.1.3
  dotenv: ^4.2.0
  google_generative_ai: ^0.4.6
  web_socket_channel: ^3.0.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
